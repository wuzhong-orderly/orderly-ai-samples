import { ConfigProvider } from "@orderly.network/react-app";

export const theme = {
  // basic custom theme
  colors: {
    primary: "#00ff00",
  }
};

export const appIcons = {
  // optional: add custom icons
};
